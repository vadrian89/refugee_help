import 'package:flutter/material.dart';

class GlobalSideMenu extends StatelessWidget {
  const GlobalSideMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
